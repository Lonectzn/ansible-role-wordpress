---

- block:
  - name: update wordpress core
    command: wp core update --path="{{ wp_web_root }}"

  - name: update wordpress plugins
    command: wp plugin update --all --path="{{ wp_web_root }}"
  tags:
    - update

