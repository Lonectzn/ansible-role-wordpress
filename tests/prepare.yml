---

- name: add a test database
  mysql_db: name=wordpress state=present

- name: add a test db user
  mysql_user: name=wordpress password=password state=present priv=wordpress.*:ALL

- name: add web root
  file: path=/var/www/html state=directory owner=www-data group=www-data
